<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <SurfaceView
        android:id="@+id/tools_magnifiter_surface"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="visible" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_gravity="bottom"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/tools_magnifiter_btn_sub"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/no_color"
            android:gravity="center"
            android:scaleType="fitCenter"
            android:src="@drawable/btn_tools_magnifiter_small" />

        <ImageButton
            android:id="@+id/tools_magnifiter_btn_add"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/no_color"
            android:gravity="center"
            android:scaleType="fitCenter"
            android:src="@drawable/btn_tools_magnifiter_big" />
    </LinearLayout>

</FrameLayout>